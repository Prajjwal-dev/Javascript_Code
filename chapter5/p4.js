const numbers = [1, 2, 3, 4, 5]; // Example array of numbers

// Use the map method to create an array of squares
const squares = numbers.map(num => num * num);

console.log("Squares of the given numbers:", squares);
